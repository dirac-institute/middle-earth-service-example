# Service account identity. Every script, Dockerfile, and compose file in this
# repo reads from here. There are no fallback defaults -- if this file is
# missing or incomplete, builds and deploys will fail.
#
# The uid/gid must match the owner of the secrets on the /service/shire NFS
# share. See SECRETS.md for why.
SVC_USER=astro-svc
SVC_UID=1725455
SVC_GID=2121725455
